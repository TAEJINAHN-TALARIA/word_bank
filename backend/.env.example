ANTHROPIC_API_KEY=your_api_key_here
PORT=3000

# Firebase Admin SDK (JSON 한 줄로 압축)
# Firebase Console → 프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성
# 생성된 JSON 파일을 한 줄로 변환: python3 -c "import json,sys; print(json.dumps(json.load(sys.stdin)))" < key.json
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"..."}

# 무료 플랜 월 조회 횟수 한도 (기본값: 50)
FREE_MONTHLY_LIMIT=50

# ─── RevenueCat 웹훅 인증 ───
# RevenueCat Dashboard → Project Settings → Integrations → Webhooks
# → "Authorization header" 필드에 임의의 강력한 시크릿 문자열을 설정하고 동일한 값을 여기에 입력
# 예: openssl rand -hex 32
REVENUECAT_WEBHOOK_AUTH_HEADER=your_webhook_secret_here
